if __name__ == "__main__":
    BASE_URL = "https://YOUR_CONFLUENCE_INSTANCE_URL"
    USERNAME = "YOUR_USERNAME"
    PASSWORD = "YOUR_PASSWORD"
    
    page_ids = ["PAGE_ID_1", "PAGE_ID_2", "PAGE_ID_3"]  # Replace with your actual page IDs

    for page_id in page_ids:
        content = get_confluence_content(BASE_URL, page_id, USERNAME, PASSWORD)
        print(f"Content for Page ID {page_id}:")
        print(content)
        print("-" * 50)  # Separator for clarity
