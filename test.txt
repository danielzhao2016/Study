screen_width = root.winfo_screenwidth()
    screen_height = root.winfo_screenheight()
    x = (screen_width / 2) - (200 / 2)  # Assuming a box width of 200
    y = (screen_height / 2) - (100 / 2)  # Assuming a box height of 100

    # Position the root window to the center of the screen
    root.geometry(f"+{int(x)}+{int(y)}")
